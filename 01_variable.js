const accountId = 12234
let accountEmail = "abc@xyz.com"
let accountPassword = "12122"

// Don't use var as the scope for var is block scope and not functional scope

console.log(accountId)
console.table([accountId, accountEmail, accountPassword])